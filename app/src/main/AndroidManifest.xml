<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/dishub_logo"
        android:label="@string/app_name"
        android:roundIcon="@id/dishubLogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrafficSafetyEducation"
        tools:targetApi="31">
        <activity
            android:name=".posttest.PosttestActivity"
            android:exported="false" />
        <activity
            android:name=".pretest.PreTestActivity"
            android:exported="false" />
        <activity
            android:name=".splash.OnBoardingActivity"
            android:exported="false" />
        <activity
            android:name=".materi.FirstVideoActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true" />
        <activity
            android:name=".materi.SecondVideoActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".splash.SplashActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>